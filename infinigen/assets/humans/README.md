# Human Assets

Place rigged character assets (`.fbx`, `.glb`, `.gltf`, `.blend`) in `infinigen/assets/humans/source/`.

These are loaded by `RiggedHumanFactory` via `register_human_actor_asset_directory`.
